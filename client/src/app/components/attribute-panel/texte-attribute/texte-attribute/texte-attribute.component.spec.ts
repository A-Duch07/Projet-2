//import { async, ComponentFixture, TestBed } from '@angular/core/testing';
//import { MatSliderChange } from '@angular/material/slider';
// import { DrawingService } from '@app/services/drawing/drawing.service';
// import { TexteService } from '@app/services/tools/texte.service';
// import { ColorService } from '@app/services/tools/color.service';
//import { CanvasTestHelper } from '@app/classes/canvas-test-helper';
//import { TexteAttributeComponent } from './texte-attribute.component';





describe('TextelAttributeComponent', () => {
    // let component: TexteAttributeComponent;
    // let fixture: ComponentFixture<TexteAttributeComponent>;
    // let drawingStub: DrawingService;
    // //let colorStub: ColorService;
    // let texteStub: TexteService;
    // let canvasTestHelper: CanvasTestHelper;
    // let baseCtxStub: CanvasRenderingContext2D;
    // let previewCtxStub: CanvasRenderingContext2D;

    //beforeEach(async(() => {
        //drawingStub = new DrawingService();
        //colorStub = new ColorService(drawingStub);
    //}));

    beforeEach(() => {
        // canvasTestHelper = TestBed.inject(CanvasTestHelper);
        // baseCtxStub = canvasTestHelper.canvas.getContext('2d') as CanvasRenderingContext2D;
        // previewCtxStub = canvasTestHelper.drawCanvas.getContext('2d') as CanvasRenderingContext2D;
        // drawingStub.baseCtx = baseCtxStub;
        // drawingStub.previewCtx = previewCtxStub;
        // fixture = TestBed.createComponent(TexteAttributeComponent);
        // component = fixture.componentInstance;
        // fixture.detectChanges();
    });

    // it('should create', () => {
    //     expect(component).toBeTruthy();
    // });

    // it('onPoliceHeightChange of texte service should change on MatSliderChange Event', () => {
    //     const matSliderChange: MatSliderChange = new MatSliderChange();
    //     matSliderChange.value = 1;
    //     const spy = spyOn(texteStub, 'onPoliceHeightChange').and.callThrough();
    //     component.policeHeightChange(matSliderChange);
    //     expect(spy).toHaveBeenCalled();
    // });

    // it('onPoliceHeightChange of texte service should not change on null event value', () => {
    //     const matSliderChange: MatSliderChange = new MatSliderChange();
    //     matSliderChange.value = null;
    //     const spy = spyOn(texteStub, 'onPoliceHeightChange').and.callThrough();
    //     component.policeHeightChange(matSliderChange);
    //     expect(spy).not.toHaveBeenCalled();
    // });
});
